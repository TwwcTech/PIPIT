namespace PIPIT
{
    public partial class MainForm : Form
    {
        public MainForm()
        {
            InitializeComponent();
        }

        private void MainForm_Load(object sender, EventArgs e)
        {
            /* + Add a "NotifyIcon" control to add icon to task bar.
             * + Add a "ContextMenuStrip" to the "NoitfyIcon", this 
             *   will allow us to diplay menu items when the user 
             *   right-clicks the taskbar icon. */
        }
    }
}